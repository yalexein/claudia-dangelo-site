---
import "../styles/collage.css";
import LanguageSwitcher from "../components/LanguageSwitcher.astro";

const { title = "Collagista" } = Astro.props;
const base = import.meta.env.BASE_URL;
---
<!doctype html>
<html lang="it">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href={`${base}favicon.svg`} />
    <link rel="stylesheet" href={`${base}site-settings.css?v=1`} />
    <script is:inline src={`${base}site-locale.js?v=1`}></script>
  </head>
  <body>
    <LanguageSwitcher />
    <header class="topbar">
      <a class="brand" href={base}>Home</a>
      <nav class="nav">
        <a href={`${base}scrittrice/`}>Scrittrice</a>
        <a aria-current="page" href={`${base}collagista/`}>Collagista</a>
      </nav>
    </header>
    <main class="container">
      <slot />
    </main>
  </body>
</html>
