<nav class="site-language-switcher" aria-label="Lingua">
  <a href="?lang=it" hreflang="it" data-locale-option="it">IT</a>
  <a href="?lang=en" hreflang="en" data-locale-option="en">EN</a>
</nav>

<style>
  .site-language-switcher {
    position: fixed;
    top: max(12px, env(safe-area-inset-top));
    right: max(12px, env(safe-area-inset-right));
    z-index: 10000;
    display: flex;
    gap: 2px;
    padding: 2px;
    border: 1px solid currentColor;
    background: color-mix(in srgb, Canvas 90%, transparent);
    color: CanvasText;
    font: 700 11px/1 "Courier New", Courier, monospace;
    letter-spacing: 0.08em;
  }

  .site-language-switcher a {
    display: grid;
    min-width: 40px;
    min-height: 40px;
    padding: 6px;
    place-items: center;
    color: inherit;
    text-decoration: none;
  }

  .site-language-switcher a:hover,
  .site-language-switcher a:focus-visible,
  .site-language-switcher a[aria-current="true"] {
    background: CanvasText;
    color: Canvas;
  }

  .site-language-switcher a:focus-visible {
    outline: 2px solid currentColor;
    outline-offset: 3px;
  }
</style>
